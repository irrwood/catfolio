(() => {
  'use strict';
  const translations = {"板块轮动": "Sector Rotation", "领先": "Leading", "减弱": "Weakening", "落后": "Lagging", "改善": "Improving", "中性": "Neutral", "动量增强": "Momentum strengthening", "动量减弱": "Momentum weakening", "动量持平": "Momentum unchanged", "历史不足": "Insufficient history", "中期与近月位置都高于板块中位数，相对表现保持在较强一侧。": "Both windows sit above the sector median; relative performance remains on the stronger side.", "中期位置较强，近月位置转弱，关注相对动量的变化。": "The medium-term position is stronger, while recent momentum has weakened.", "中期与近月位置都低于板块中位数，相对表现仍偏弱。": "Both windows sit below the sector median; relative performance remains weaker.", "中期位置偏弱，近月位置较强，相对动量正在改善。": "The medium-term position is weaker, while recent relative momentum is improving.", "位置接近截面中心，板块之间的强弱差异尚不鲜明。": "The position is near the cross-sectional centre, with no clear relative strength distinction.", "较上周从「%@」转为「%@」。": "Changed from %@ to %@ since last week.", "中期相对强弱 × 近月相对动量": "Medium-term strength × recent momentum", "如何阅读这张图": "How to read this chart", "短期": "Short term", "中期": "Medium term", "长期": "Long term", "数据截至 %@（纽约）": "Data as of %@ (New York)", "数据更新延迟，当前显示上次可用快照。": "Update delayed. Showing the last available snapshot.", "回填快照": "Backfilled snapshot", "近 1 月相对动量 ↑": "Recent relative momentum ↑", "中期相对强弱 →": "Medium-term relative strength →", "图心是当日 11 个板块的中位数，不是 SPY 或零收益。位置是相对其他板块的，不是绝对涨跌。": "The centre is the daily median of 11 sectors, not SPY or zero return. Positions are relative to other sectors, not absolute gains or losses.", "读取每日快照…": "Loading daily snapshot…", "暂无板块轮动数据": "No sector rotation data", "连接快照服务后读取每日板块数据。": "Connect a snapshot service to read daily sector data.", "中期相对强弱": "Medium-term relative strength", "近 1 月相对动量": "Recent relative momentum", "本周变化": "Weekly change", "点击圆点或板块列表查看 8 周轨迹，长按圆点查看精确值。": "Tap a point or sector to see its 8-week trail. Hold a point for precise values.", "展示板块相对 SPY 的趋势和动量，仅供市场观察，不构成投资建议。": "Sector trends and momentum relative to SPY, for market observation only. Not investment advice.", "快照数据源": "Snapshot source", "中期窗口为第 63 至第 21 个交易日前；近月窗口为最近 21 个交易日。先计算相对 SPY 的对数差并作 5 日均值，再用当日中位数与 MAD 标准化及 tanh 压缩。": "The medium-term window runs from 63 to 21 sessions ago; momentum covers the latest 21 sessions. Log price ratios to SPY are averaged over 5 sessions, normalised using the daily median and MAD, then compressed with tanh.", "中心 ±0.25 范围为中性。新象限连续两交易日成立才切换文字标签，文字可能暂时不同于点所在象限。百分比表示相对 SPY 的变化；坐标表示相对其他板块的位置。": "The central ±0.25 zone is neutral. A new quadrant needs two consecutive sessions to change the label, which can temporarily differ from the plotted quadrant. Percentages measure change relative to SPY; coordinates compare sectors.", "轨迹读取历史快照，每个完整 ISO 周取最后有效交易日。初始化历史使用回填时可得的复权价，可能与当时发布值略有差异。此图不使用 JdK RRG 专有计算。": "Trails read saved snapshots at the last valid session of each completed ISO week. Initial backfills use adjusted prices available at backfill time and may differ slightly from original observations. This chart does not use proprietary JdK RRG calculations.", "填写已部署的 HTTPS 快照 API。未连接时保留本机快照，行情过期会明确标记。此读取不触发账户同步或行情计算。": "Enter a deployed HTTPS snapshot API. Saved snapshots remain available offline and expired data is marked. Reading does not trigger account sync or market calculations.", "精确值": "Precise values", "暂停历史回放": "Pause history playback", "播放历史快照": "Play historical snapshots", "回看日期": "History date", "美国市场 · 11 个 SPDR 板块 ETF / SPY": "US market · 11 SPDR sector ETFs / SPY", "观察周期": "Observation period", "暂未开放": "Not available yet", "板块轮动四象限图": "Sector rotation quadrant chart", "播放": "Play", "暂停": "Pause", "最新": "Latest", "选择一个板块": "Select a sector", "点击图中圆点或下方列表，查看过去 8 周轨迹与规则解释。": "Select a point or sector below to see its past 8 weeks and rule-based explanation.", "全部板块": "All sectors", "领先：中期与近月位置都高于板块中位数。减弱：中期高于、近月低于。落后：两者都低于。改善：中期低于、近月高于。这些状态不表示绝对涨跌，也不是买卖信号。": "Leading: both windows above the sector median. Weakening: medium-term above, recent below. Lagging: both below. Improving: medium-term below, recent above. These states do not describe absolute returns or trading signals.", "先计算 ETF 与 SPY 复权价的对数差，再作 5 日均值。横轴取第 63 至第 21 个交易日前的变化，纵轴取最近 21 个交易日的变化；两者不重叠。分别以当日中位数和 MAD 标准化，再用 tanh 软压缩。": "Log ratios of adjusted ETF prices to SPY are averaged over 5 sessions. The horizontal axis measures change from 63 to 21 sessions ago; the vertical axis measures the latest 21 sessions. These windows do not overlap. Each axis uses daily median/MAD normalisation and tanh compression.", "显示的百分比由原始对数差还原，表示相对 SPY 的变化。图上位置以板块截面为中心。中心 ±0.25 范围为中性；新象限连续两交易日成立才切换文字标签，因此文字可能暂时不同于点所在象限。": "Percentages convert the raw log differences back into returns relative to SPY. Coordinates are centred on the sector cross-section. The central ±0.25 zone is neutral; other labels change after two consecutive sessions, so labels may temporarily differ from plotted quadrants.", "历史来自当日快照，每周取最后有效交易日；选中后显示当前点和过去 8 个完整 ISO 周的周点。初始化历史使用回填时可得的复权序列，可能与当时实际发布值略有差异。此图不使用 JdK RRG 专有计算。": "History comes from saved daily snapshots. Selection shows the current point plus the last valid session of each of the previous 8 completed ISO weeks. Initial backfills use adjusted prices available at backfill time and may differ from original observations. This chart does not use proprietary JdK RRG calculations.", "观察 11 个美股板块的中期相对强弱与近月动量。": "Observe medium-term relative strength and recent momentum across 11 US sectors.", "查看四象限与历史轨迹 →": "View quadrants and historical trails →", "尚无可用快照；等待每日批处理完成。": "No snapshots yet. Waiting for the daily batch.", "无法读取快照，请稍后重试。": "Unable to read snapshots. Please try again."};
  const en = document.documentElement.lang === "en";
  const T = text => en ? (translations[text] || text) : text;
  const $ = id => document.getElementById(id);
  const symbols = ['XLK','XLF','XLE','XLV','XLY','XLP','XLI','XLB','XLU','XLRE','XLC'];
  const colors = ['#708cff','#ef9a60','#aa82e8','#de83a5','#58adbc','#a4aa70','#667f9c','#bd9071','#9095ce','#bb9bc4','#718f84'];
  const labels = {leading:'领先',weakening:'减弱',lagging:'落后',improving:'改善',neutral:'中性'};
  const weekly = {strengthening:'动量增强',weakening:'动量减弱',unchanged:'动量持平',unavailable:'历史不足'};
  const copy = {leading:'中期与近月位置都高于板块中位数，相对表现保持在较强一侧。',weakening:'中期位置较强，近月位置转弱，关注相对动量的变化。',lagging:'中期与近月位置都低于板块中位数，相对表现仍偏弱。',improving:'中期位置偏弱，近月位置较强，相对动量正在改善。',neutral:'位置接近截面中心，板块之间的强弱差异尚不鲜明。'};
  let data, selected = null, dates = [], index = 0, timer = null, generation = 0, playbackRun = 0, loadFailed = false;
  const plot = $('rotation-plot');
  const coord = (x,y) => [8+(x+2.5)/5*84, 12+(2.5-y)/5*76];
  const pct = n => `${n>=0?'+':''}${(n*100).toFixed(1)}%`;
  const el = (tag,text,cls) => { const n=document.createElement(tag); if(text !== undefined)n.textContent=T(text);if(cls)n.className=cls;return n; };
  function stop(){playbackRun++;clearTimeout(timer);timer=null;$('rotation-play').textContent=T('播放');$('rotation-play').setAttribute('aria-label','播放历史快照');}
  function render(){
    plot.replaceChildren();$('rotation-sectors').replaceChildren();
    const ns='http://www.w3.org/2000/svg';
    const svg=document.createElementNS(ns,'svg');svg.setAttribute('viewBox','0 0 600 400');svg.setAttribute('preserveAspectRatio','none');svg.setAttribute('aria-hidden','true');
    const draw=(tag,attrs)=>{const n=document.createElementNS(ns,tag);Object.entries(attrs).forEach(([k,v])=>n.setAttribute(k,v));svg.append(n);return n;};
    [-2,-1,0,1,2].forEach(v=>{const [x,y]=coord(v,v);draw('line',{x1:x*6,y1:0,x2:x*6,y2:400,class:v===0?'rotation-center':'rotation-grid'});draw('line',{x1:0,y1:y*4,x2:600,y2:y*4,class:v===0?'rotation-center':'rotation-grid'});});
    [['改善',16,24],['领先',584,24],['落后',16,382],['减弱',584,382]].forEach(([t,x,y])=>{const n=draw('text',{x,y,'text-anchor':x>300?'end':'start'});n.textContent=T(t);n.style.fontSize=`${Math.max(10,10*600/plot.clientWidth)}px`;});
    const sector=data.sectors.find(s=>s.symbol===selected);
    if(sector){
      const points=[...sector.trail,{date:data.asOf,x:sector.x,y:sector.y}];const color=colors[symbols.indexOf(selected)];
      points.forEach((p,i)=>{const [x,y]=coord(p.x,p.y);if(i){const [px,py]=coord(points[i-1].x,points[i-1].y);draw('line',{x1:px*6,y1:py*4,x2:x*6,y2:y*4,stroke:color,'stroke-width':1.8,opacity:.25+.75*i/(points.length-1)});}if(i<points.length-1)draw('circle',{cx:x*6,cy:y*4,r:2+i*.4,fill:color,opacity:.2+.7*i/points.length});});
      if(points.length>1){const [x,y]=coord(sector.x,sector.y),[px,py]=coord(points.at(-2).x,points.at(-2).y);const a=Math.atan2((y-py)*4,(x-px)*6);if(Math.hypot(x-px,y-py)>.05){const ax=x*6-10*Math.cos(a),ay=y*4-10*Math.sin(a);draw('polyline',{points:`${ax-7*Math.cos(a-.5)},${ay-7*Math.sin(a-.5)} ${ax},${ay} ${ax-7*Math.cos(a+.5)},${ay-7*Math.sin(a+.5)}`,fill:'none',stroke:color,'stroke-width':2});}}
    }
    plot.append(svg);
    const labelSet=new Set([...data.sectors].sort((a,b)=>b.x*b.x+b.y*b.y-a.x*a.x-a.y*a.y).slice(0,4).map(s=>s.symbol));
    const boxes=[];
    data.sectors.forEach(s=>{
      const color=colors[symbols.indexOf(s.symbol)], [x,y]=coord(s.x,s.y);
      const b=el('button',undefined,'rotation-point');b.type='button';b.style.left=`${x}%`;b.style.top=`${y}%`;b.style.setProperty('--sector-color',color);b.setAttribute('aria-label',`${s.nameZh} ${s.symbol} ${s.quadrantLabel}，中期 ${pct(s.relativeTrend)}，近月 ${pct(s.relativeMomentum)}`);b.setAttribute('aria-pressed',String(selected===s.symbol));b.title=`${s.symbol} · ${s.quadrantLabel}\nx=${s.x.toFixed(3)}, y=${s.y.toFixed(3)}\n中期 ${pct(s.relativeTrend)} · 近月 ${pct(s.relativeMomentum)}`;
      b.append(el('i'));if(labelSet.has(s.symbol)||selected===s.symbol){const label=el('span',s.symbol);const px=x/100*plot.clientWidth,py=y/100*plot.clientHeight;let offset=16;for(let trial=0;trial<4;trial++){const ny=py+offset;if(!boxes.some(([bx,by])=>Math.abs(bx-px)<42&&Math.abs(by-ny)<16)&&!data.sectors.some(other=>{if(other.symbol===s.symbol)return false;const [ox,oy]=coord(other.x,other.y);return Math.abs(ox/100*plot.clientWidth-px)<24&&Math.abs(oy/100*plot.clientHeight-ny)<12;})){boxes.push([px,ny]);break;}offset=trial%2===0?-20-trial*8:24+trial*8;}label.style.top=`${22+offset}px`;b.append(label);}
      const choose=()=>{selected=selected===s.symbol?null:s.symbol;render();};b.onclick=e=>{e.stopPropagation();if(e.detail===0){choose();return;}chooseAt(e.clientX,e.clientY);};plot.append(b);
      const item=el('button');item.type='button';item.style.setProperty('--sector-color',color);item.setAttribute('aria-pressed',String(selected===s.symbol));item.append(el('i',undefined,'rotation-dot'),document.createTextNode(`${en?s.name:s.nameZh} · ${s.symbol} · ${T(s.quadrantLabel)}`));item.onclick=choose;$('rotation-sectors').append(item);
    });
    const detail=$('rotation-detail');detail.replaceChildren();
    if(sector){detail.append(el('h3',`${en?sector.name:sector.nameZh} · ${sector.symbol}`),el('p',sector.quadrantLabel));const dl=el('dl');[['中期相对强弱',pct(sector.relativeTrend)],['近 1 月相对动量',pct(sector.relativeMomentum)],['本周变化',weekly[sector.weeklyChange]]].forEach(([k,v])=>dl.append(el('dt',k),el('dd',v)));detail.append(dl);let explanation=T(copy[sector.quadrant]);if(sector.previousQuadrant&&sector.previousQuadrant!==sector.quadrant)explanation=(en?`Changed from ${T(labels[sector.previousQuadrant])} to ${T(sector.quadrantLabel)} since last week. `:`较上周从「${labels[sector.previousQuadrant]}」转为「${sector.quadrantLabel}」。`)+explanation;detail.append(el('p',explanation),el('p',en?`Coordinates x ${sector.x.toFixed(3)} / y ${sector.y.toFixed(3)} · ${sector.trail.length} weekly points`:`精确坐标 x ${sector.x.toFixed(3)} / y ${sector.y.toFixed(3)} · ${sector.trail.length} 个历史周点`));}
    else detail.append(el('h3','选择一个板块'),el('p','点击图中圆点或下方列表，查看过去 8 周轨迹与规则解释。'));
    $('rotation-status').textContent=en?`${(data.stale||loadFailed||Date.now()>=Date.parse(data.validUntil))?'Update delayed · ':''}${data.historical?'History · ':''}Data as of ${data.asOf} (New York)${data.backfilled?' · Backfilled':''}`:`${(data.stale||loadFailed||Date.now()>=Date.parse(data.validUntil))?'数据更新延迟 · ':''}${data.historical?'历史回看 · ':''}数据截至 ${data.asOf}（纽约）${data.backfilled?' · 回填快照':''}`;
    $('rotation-date').value=index;$('rotation-date-label').textContent=data.asOf;
  }
  async function load(asOf){const g=++generation;try{const res=await fetch('/api/sector-rotation'+(asOf?'?asOf='+encodeURIComponent(asOf):''));if(!res.ok)throw Error();const incoming=await res.json();if(g!==generation)return;if(incoming.status!=='available'){$('rotation-status').textContent=T('尚无可用快照；等待每日批处理完成。');plot.replaceChildren(el('p','暂无板块轮动数据','rotation-empty'));return;}if(incoming.calcVersion!==2||incoming.sectors.length!==11)throw Error();data=incoming;loadFailed=false;if(!dates.length){dates=data.dates;index=dates.length-1;$('rotation-date').max=index;['rotation-date','rotation-play','rotation-latest'].forEach(id=>$(id).disabled=dates.length<2);}render();}catch(e){if(g===generation){loadFailed=true;stop();$('rotation-status').textContent=data?(en?`Update failed · Data as of ${data.asOf} (New York)`:`更新失败 · 数据截至 ${data.asOf}（纽约）`):T('无法读取快照，请稍后重试。');}}}
  function chooseAt(clientX,clientY){if(!data)return;const rect=plot.getBoundingClientRect();let best=null,distance=22;data.sectors.forEach(s=>{const [x,y]=coord(s.x,s.y);const d=Math.hypot(clientX-rect.left-x/100*rect.width,clientY-rect.top-y/100*rect.height);if(d<distance){distance=d;best=s.symbol;}});selected=selected===best?null:best;render();}
  plot.onclick=e=>chooseAt(e.clientX,e.clientY);
  $('rotation-date').oninput=()=>{stop();index=Number($('rotation-date').value);load(dates[index]);};
  $('rotation-latest').onclick=()=>{stop();index=dates.length-1;load(dates[index]);};
  $('rotation-play').onclick=()=>{if(timer){stop();return;}if(index===dates.length-1)index=-1;const run=++playbackRun;const next=async()=>{if(run!==playbackRun)return;index++;await load(dates[index]);if(run!==playbackRun)return;if(index>=dates.length-1){stop();return;}timer=setTimeout(next,900);};$('rotation-play').textContent=T('暂停');$('rotation-play').setAttribute('aria-label','暂停历史回放');timer=setTimeout(next,0);};
  document.addEventListener('visibilitychange',()=>{if(document.hidden)stop();});
  load();
})();
